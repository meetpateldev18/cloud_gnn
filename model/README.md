# Model Weights Directory

Place your trained model file here after training on Kaggle:

  scheduler_model.pt

The backend will automatically load this file on startup.
If the file is missing, the system uses a heuristic fallback scheduler.
